package com.dbschools.mgb.schema

/**
 * Helper object used feed DB with initial data.
 * @since 1.0.0
 */
object DataHelper {
  /**
   * Creates some initial data for DB feeding.
   */
  def createInitialData = {
    // TODO: implement it when finished the data model
  }
}
